@model IEnumerable<OderDetail>
@{
    ViewData["title"] = "Order Details";
}
<h3>List Order</h3>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Oder Code</th>
            <th scope="col">Oder Name</th>
            <th scope="col">Oder Date</th>
            <th scope="col">Oder Status</th>


        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <th>#</th>
                <th scope="row">@item.OrderCode</th>
       @*          <td>@item.UserName</td>
                <td>@item.CreateDate</td>

                <td>
                    @if (@item.Status == 1)
                    {
                        <span class="text text-success">Đơn hàng mới</span>
                    }
                    else
                    {
                        <span class="text text-danger">Đơn hàng thành công</span>
                    }

                </td> *@



            </tr>

        }


    </tbody>
</table>


